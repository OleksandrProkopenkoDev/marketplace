spring:
  application:
    name: marketplace

  profiles:
    active: local

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    flyway:
      enabled: true
      baseline-on-migrate: true
      validate-on-migrate: true